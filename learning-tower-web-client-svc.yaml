apiVersion: v1
kind: Service
metadata:
    name: learning-tower-web-client
    labels:
        run: learning-tower-web-client
spec:
    ports:
    - port: 8000
      protocol: TCP
    selector:
        run: learning-tower-web-client
